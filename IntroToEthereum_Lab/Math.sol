pragma solidity 0.4.24;

contract AddNumbers {
    function AddNum(uint256 a, uint256 b) public returns (uint256){
        return a + b;
    }
}

contract Subtract {
    function Sub(uint256 a, uint256 b) public returns (uint256){
       return a - b;
    }
}
